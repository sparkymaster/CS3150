#ifndef COURT
#define COURT

#include "Structs_and_enum.h"
#include "Ball.h"

int randomInt(int, int);
void fillArray(Player, Player, Ball[], int);
void print_array();
// void found_empty(hero);
// hero found_spike(hero);
// hero found_qs(hero);
// void found_insult(hero);
// hero found_good_potion(hero);
// hero found_bad_potion(hero);
// void found_set_entrance(hero);
// int get_tile(hero);


#endif
